# methods
MCP_M_DS_SETTINGS=这是一个辅助接口，用于获取 CloudDM 支持的数据源类型及其特性信息。具体包括：\
\n- 数据源类型\
\n- 数据源所属分类\
\n- 拼写敏感性（如：大写、小写、大小写混合或未指定）\
\n- SQL 生成时用于包裹数据库对象的左右限定符\
\n- 数据库组织结构层级（例如 Catalog > Schema > Table 或其他变体）\
\n此接口有助于在不同数据库环境下准确地生成 SQL 语句，并确保数据源的正确配置。
MCP_M_LIST_DS=查询数据源列表，这个接口会返回查询数据源的 `数字 ID` 和 `实例 ID`，这两个 ID 及其重要展示时放在前面。数字 ID 通常会参与后续其它接口的调用操作。\
  该接口的查询参数在模糊查询时不支持或的关系，例如 instanceIdLike 和 instanceDescLike 在同时指定的时候是一个与关系而非或关系。因此建议使用单个条件进行匹配。只有在遇到需要更精确的过滤时，才同时使用多个条件进行过滤。
MCP_M_DETAIL_DS=查询数据源详细信息，它需要一个 `数字 ID` 查询某一个数据源的具体信息，包括连接串，所属环境等信息。
MCP_M_LIST_LEVELS=查询数据源的组织结构层级列表。\
\n- 返回：按入参返回对应层级列表；入参为空返回所有环境，传入环境返回其下实例，依次类推。\
\n- 末端：到达 Catalog 或 Schema 时，返回表列表。\
\n- 返回结构恒定：\
\n  - objName：名称；\
\n  - objId：对象的数字 ID（部分场景不可靠，值为 -1 表示无意义）；\
\n  - objAlias：别名；在实例列表中为 InstanceDesc；\
\n  - objType：对象类型；数据库对象中定义类型丰富，其中最主要的有（Env、Instance、RdbCatalog、RdbExternalCatalog、RdbSchema、RdbExternalSchema、RdbTable、RdbExternalTable、RdbView、RdbMaterialized、RdbColumn）\
\n  - objAttr：Map。环境/实例包含较固定字段（如 dsType、status、dsInstance、dsInstanceDesc、dsEnvId）；Catalog/Schema/Table 包含更多与数据源相关的扩展属性，可能含“_tips”用于简略展示。
MCP_M_LIST_LEAF=根据对象类型，查询数据源的组织结构层级最末端下的对象列表。可以是表、视图、存储过程、存储函数、序列、等数据库对象。\
  这个接口需要指定层级路径和对象类型来获取对应的对象列表，它的局限性是不能查看某个对象的详情，例如查看表具有的列是不支持的。
MCP_M_EXECUTE_QUERY=从数据库查询数据，执行用户传入的 SQL 语句，并返回查询结果。\n- 支持多条 SQL 语句执行，语句间用分号分隔。\
\n- 支持查询计划查看，通过指定查询类型为 RequestPlan 来获取查询计划。\
\n- 支持规则检查，返回 ruleCheck 参数来查看规则检查结果，用户可根据结果决定是否继续执行查询。\
\n- 适用于各种数据库类型。\
\n- 在结果显示时需要明确告诉用户执行的具体 SQL（script 字段） 如果有改写动作需要明确提示用户改写的部分（通过对比 original和script 的差异）\
\n- rewriteTags 字段决定了改写的类型，如果用户需要明确知道改写的内容和原因可以提示这个信息。\
\n- receiveCost 提示给用户整个数据接收的时间有多长。\
\n- fetchCount 字段表示有多少数据被查询到了。
#
#
# fields
MCP_F_DATA_SOURCE_ID_DESC=这个字段表示 `数字 ID`，是 Java 的 Long 类型，每个数据源都有唯一的 `数字 ID`。\
\n- 功能与 `实例 ID` 相同，但形式不同：\
\n- `数字 ID` 随使用增长，进位后字符串长度会增加。\
\n- `实例 ID` 是固定长度的、具有辨识度的字符串。\
\n- 在列表展示时，该字段应放在前面以方便标识。如果同时展示 `数字 ID` 和 `实例 ID`，优先展示 `实例 ID`。\
\n- 作为参数传入时，需与 `实例 ID` 区分，以免混淆。
MCP_F_DATA_SOURCE_TYPE_DESC=数据源类型，例如 MySQL、PostgreSQL、Oracle 等。每个数据源有且仅有一个确定的类型。不同类型的数据源在连接方式和查询语法等方面有所不同。\
\n- 在列表展示时，该字段应放在前面以凸显数据源类型，通常位于数据源 ID 之后。\
\n- 作为参数传入时：\
\n- 需注意拼写，接口会严格校验。\
\n- 一般使用上一个接口输出的数据源类型参数。\
\n- 如果没有上游接口提供数据源类型，可以通过 dsSettings 工具获取支持的数据源类型信息，并推断具体的数据源类型。
MCP_F_INSTANCE_ID_DESC=这个字段表示 `实例 ID`，是 Java 的 String 类型，每个数据源都有唯一的 `实例 ID`。实例 ID 具有一定的辨识度，通常其前几个字符表示数据源的类型。\
\n- 功能与 `数字 ID` 相同，但形式不同：\
\n- `实例 ID` 是固定长度、具有辨识度的字符串。\
\n- `数字 ID` 是纯数字，随着使用增长，进位后字符串长度会增加，而 `实例 ID` 不会有这种情况。\
\n- 在列表展示时，该字段应放在前面以方便标识。如果同时展示 `数字 ID` 和 `实例 ID`，优先展示 `实例 ID`。\
\n- 作为参数传入时，需与 `数字 ID` 区分，以免混淆。
MCP_F_INSTANCE_ID_LIKE_DESC=这个字段是 Java 的 String 类型，通常由用户传入，用于表示 `实例 ID` 或其部分内容。\
\n- 在一般列表查询中：如果结合用户输入可以明确确定一个数据源的 `数字 ID`，应优先使用 `数字 ID` 参数。否则，可以使用该字段进行模糊搜索。
MCP_F_INSTANCE_DESC_LIKE_DESC=Java String 类型，用户传入，用于标识“实例备注”。备注可被后续修改，与“实例 ID”不同。默认备注可能重复，使用时需注意区分或做模糊匹配。
MCP_F_HOST_LIKE_DESC=Java String 类型，表示数据源连接的服务器地址，可为 IP 或域名。支持列表查询的模糊搜索，匹配包含该字符串的所有地址。
MCP_F_LEVELS_DESC=表示要查询的目标数据库组织层级（环境的数字ID / 数据源数字ID / 库 Catalog / 模式 Schema / 表 Table）参数顺序要求按从上到下的顺序排列各层级名称或ID。\
\n- 约束：各级入参须来源于上一级输出；不支持包含“/”的名称，遇到时应提示或报错。\
\n- 参数举例：\
\n  - 当获取数据源列表时，需要传入 环境的数字ID，例如： 1、\
\n  - 当获取 Schema 列表时需要传入环境ID 和数据源数字ID，例如 1 / 2\。层级信息按照字符串数组形式定义，。
MCP_F_REFRESH_CACHE_DESC=是否刷新缓存，Java Boolean 类型。
MCP_F_LEAF_TYPE_DESC=表示要查询的“最末端层级”对象类型，用于限定返回的对象列表。可为表、外部表、视图、物化视图、序列、同义词、存储过程、存储函数等；常用枚举：Table、ExternalTable、View、Materialized、Sequence、Synonym。\
\n- 取值受数据源类型限制；以 dsSettings 接口的 leafExpand 为准。\
\n- 与 listLeaf 搭配使用，仅返回指定类型的对象。
MCP_F_LEAF_EXPAND_DESC=表示该数据源“末端层级”可返回对象的支持类型列表。\
\n\- 用途：供 listLeaf 接口作为可选类型集合与校验依据。\
\n\- 来源：由 dsSettings.leafExpand 提供，随数据源类型而变化。\
\n\- 示例：Table、ExternalTable、View、Materialized、Sequence、Synonym 等。
MCP_F_QUERY_TYPE_DESC=表示查询的类型，Java 的 String 类型。取值范围：\
  - RequestQuery 表示一个查询操作，可以执行任意 SQL 语句通常是查询。\
  - RequestPlan，当需要查看查询计划时使用该语句，因为有些数据源查询计划的查看可能无法通过一个 SQL 来完成。该模式下 CloudDM 可以有效的解决这个问题，它底层对于查询计划请求有专门优化。
MCP_F_QUERY_STRING_DESC=SQL 查询命令，可以是任意合法的查询语句，支持多条语句执行，语句间用分号分隔，有些数据源可能使用其它类型分隔符，例如 SQL SERVER 会使用 GO。但是绝大情况下分号是通用分隔符。\
\n- 注意事项：不支持查询语句中包含切换 Schema/Catalog 的命令，如 USE database_name；如有需要需要再另外一个查询，并通过 levels 参数决定。
MCP_F_QUERY_FORCE_DESC=是否强制执行查询，Java Boolean 类型。首次查询一般使用 false。\
\n- 当查询的返回信息 ruleCheckLevel 字段不为 PASS 时需要用户来决定是否需要忽略提示继续执行，此时参数值应该为 true。\
\n- 如果 ruleCheckLevel 值为 SUGGEST 表示还可以通过设置为 true 来决定继续执行。\
\n- 如果 ruleCheckLevel 值为 FAILURE 则不用再执行了，以一个错误形式告诉用户 ruleCheckList 中没有满足的规则。
MCP_M_DBCHANGE_TICKETS=按数据库名查询数据库变更工单列表。通过数据库名匹配预发/生产两侧的库映射关系（dm_db_pair），返回关联的治理工单（含工单类型、标题、服务、库列表、语句摘要、执行状态、是否已转产等）。适用于钉钉发布流程勾选"本次发布存在数据库变更"时查询。
MCP_M_DBCHANGE_STATEMENTS=按工单 ID 查询数据库变更工单的语句详情与预检结果。返回每个库的语句内容、预检结果 JSON、执行状态与详情。适用于钉钉审批流展示工单明细。
MCP_M_DBCHANGE_RELEASE=按发布单 ID 查询生产发布单详情。返回发布单号、标题、状态、按生产库分组的语句快照（含 hash、执行状态）、事件时间线。适用于钉钉审批流按发布单拉取完整明细。
MCP_F_DB_NAME_DESC=数据库名称，Java String 类型。用于按库名查询变更工单。库名会同时匹配预发侧和生产侧的库映射关系（dm_db_pair 的 pre_db_name 或 prod_db_name），因此传入预发库名或生产库名均可查询到关联工单。
MCP_F_TICKET_TYPE_DESC=工单类型，Java String 类型，可选值：PRE_DDL（预发 DDL 工单）、PROD_DML（生产 DML 工单）。不传则返回所有类型。
MCP_F_EXECUTED_ONLY_DESC=是否只返回已执行的工单，Java Boolean 类型，默认 false。为 true 时仅返回工单状态为 FINISHED 且所有语句组执行成功的工单。
MCP_F_PAGE_DESC=页码，Java int 类型，从 1 开始，默认 1。
MCP_F_SIZE_DESC=每页条数，Java int 类型，默认 20。
MCP_F_TICKET_ID_DESC=工单 ID，Java Long 类型。用于查询指定工单的语句详情与预检结果。
MCP_F_RELEASE_ID_DESC=发布单 ID，Java Long 类型。用于查询指定发布单的详情。